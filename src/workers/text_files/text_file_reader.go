package text_files

func getFileContents(file string) string {
	/*jsonFile, err := os.Open(file)
	if err != nil {
		log.Fatal(err)
	}
	defer jsonFile.Close()
	fileContents := bufio.NewScanner(jsonFile)
	return fileContents*/
	return "don"
}
